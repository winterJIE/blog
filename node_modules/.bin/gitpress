#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gitpress/bin/gitpress" "$@"
  ret=$?
else 
  node  "$basedir/../gitpress/bin/gitpress" "$@"
  ret=$?
fi
exit $ret
